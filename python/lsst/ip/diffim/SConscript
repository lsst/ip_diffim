# -*- python -*-
Import("env")
import lsst.SConsUtils as scons

env.CheckSwig("python", ilang="c++", includedProducts="afw daf_base pex_logging detection")

env.LoadableModuleIncomplete("_diffimLib", Split("diffimLib.i"),
                             LIBS=env.getlibs("ip_diffim"))
