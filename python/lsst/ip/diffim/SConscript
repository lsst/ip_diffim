# -*- python -*-
Import("env")
import lsst.SConsUtils as scons

# includedProducts lists packages with included .i files
env.CheckSwig("python", ilang="c++", includedProducts="utils afw detection daf_base")

env.LoadableModuleIncomplete("_diffimLib", "diffimLib.i", LIBS=env.getlibs("ip_diffim"))
