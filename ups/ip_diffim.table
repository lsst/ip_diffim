setupRequired(daf_base 3.0)
setupRequired(pex_logging 3.0)
setupRequired(pex_exceptions 3.0)
setupRequired(pex_policy 3.0.1)
setupRequired(daf_persistence 3.1.1)
setupRequired(afw 3.0)
setupRequired(detection 3.0.1)
setupRequired(vwlapack >= 1.0.0)

setupOptional(pex_harness 3.0.1)
setupOptional(afwdata)

envPrepend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envPrepend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
