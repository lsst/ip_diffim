# -*- python -*-
Import('env')

env.SharedLibraryIncomplete(env['eups_product'], map(lambda str: "../src/" + str,
                                           [
    "ImageSubtract.cc",
    "PCA.cc",
    "wcsMatch.cc",
                                            ])
                            #, LIBS=env.getlibs("daf_base pex_policy afw boost wcslib detection")
                            )
